#!/bin/sh
dotfiles=".bash_aliases .bashrc .gitconfig .hgrc .synergy.conf .vimrc"
cd $HOME
for f in $dotfiles; do
    if [ "$f" != ".git" ]; then
        echo "ln -sf dotfiles/$f";
        ln -sf dotfiles/$f;
    fi;
done;
